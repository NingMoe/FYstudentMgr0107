<div id="info" ></div>
@*<div id="chartContainer0" style="height: 400px; width: 100%;"></div>
<div id="chartContainer1" style="height: 400px; width: 100%;"></div>*@
@section Scripts {
    <script src="~/Scripts/canvasjs.min.js"></script>
    <script type="text/javascript">

    $(function () {
        var data =@Html.Raw(ViewBag.SeriesData);
            $.each(data, function(i, item) {
                var chart="chartContainer"+i;
                //alert( "<div id=\'"+chart+"\' style='height: 400px; width: 100%;'></div>");
                $("#info").append(
                  "<div id=\'"+chart+"\' style='height: 400px; width: 100%;'></div>"
                 );
                //alert(chart);
                //alert(item.Title);
                //alert(item.dataPoints);            
                eval("var chart = new CanvasJS.Chart(\""+chart+"\",{theme: \"theme4\",title: { text:\""+ item.Title+"各楼栋学生分布情况"+"\"},data:[{click: onClick,cursor: \"pointer\",type: \"column\",dataPoints: "+ item.dataPoints+"}]});");

                chart.render();

            });






            function onClick(e) {
                alert("Type: " + e.dataSeries.type + ", dataPoint { x:" + e.dataPoint.label + ", y: " + e.dataPoint.y + " }");
            };
            @*var chart = new CanvasJS.Chart("chartContainer",
             {
                 title: {
                     text: "各学校学生分布情况"
                 },
                 data: [
                 {
                     click: onClick,
                     cursor: "pointer",
                     type: "column",
                     dataPoints: @Html.Raw(ViewBag.DataPoints),
                 },
                         {
                             click: onClick,
                             cursor: "pointer",
                             type: "line",
                             dataPoints: @Html.Raw(ViewBag.DataPoints),
                         }]
             });

            chart.render();*@
    });
</script>
}

